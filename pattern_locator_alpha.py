import collections

def pattern_word_locator(html_dump):
    examples = []
    html_dump = re.sub(r'+',' ',re.sub(r',','',html_dump)).lower()
    html_dump = html_dump.split()
    loop = 1
    #research 3d arrays
    for i in html_dump:
        words = words + (for i in examples)[-1],(for i in examples)[1]
    possible_pattern_word = (words).most_common(50))
    return(possible_pattern_word)
#github test
